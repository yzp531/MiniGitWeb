# Mini Git Web (极速切换版) - 产品需求文档 (PRD)

## 1. 项目概述 
### 1.1 背景
为了防止无关人员通过局域网误入系统，本工具需具备网页登录锁；为了提高通用性，系统支持动态输入或切换本地的任何 Git 仓储目录。在此基础上，**为了免去用户每次手动复制粘贴长路径的麻烦，系统需支持“便捷操作按钮”（常用目录快捷键）功能**。

### 1.2 目标
构建一个具备 **Web 访问安全登录**、**支持多仓库目录动态切换**、且**拥有常用路径便捷一键切换按钮**的极简网页端 Git 客户端。保留核心的 Status、Add、Pull、Push 功能，提供极速、无脑化的跨项目管理体验。

---

## 2. 业务流程与快捷机制
### 2.1 便捷操作流
[输入 Web 地址] ➔ [登录验证] ➔ [进入控制台] ➔ **[点击界面上的“便捷操作按钮（如：项目A）”]** ➔ [前端自动填入并切换目录] ➔ [后端验证通过] ➔ [自动刷新当前项目 Git 状态]

### 2.2 便捷按钮（记忆机制）设计
*   **免库存储**：当用户在输入框**手动成功挂载**一个新路径（后端验证通过）后，前端通过 JavaScript 自动将该路径追加存入浏览器的 `localStorage`（如键名：`git_history_paths`）。
*   **动态渲染**：每次刷新或进入控制台页面时，前端自动读取该列表，并在路径输入框下方渲染出一排**“便捷操作按钮”**。
*   **一键清除/管理**：每个便捷按钮旁边配有微型删除图标（×），用户可随时将不再维护的项目移出便捷列表。

---

## 3. 功能需求矩阵

### 3.1 核心功能说明

| 功能模块 | 功能名称 | 技术逻辑 / 对应命令 | 详细业务逻辑与行为 |
| :--- | :--- | :--- | :--- |
| **Web 登录** | **网站访问登录** | `POST /api/auth/login` | 输入账号密码，与后端固定值对比。成功发放 Token，失败则提示。 |
| **路径管理** | **切换文件目录** | `POST /api/git/validate` | 1. 用户输入或通过便捷按钮触发本地仓库的绝对路径（如 `/Users/work/project1`）。<br>2. 后端检查该路径存在且包含 `.git` 文件夹。<br>3. 验证通过：返回成功，前端将该路径设为当前激活目录，**并将其永久存入前端便捷按钮缓存**，自动触发状态刷新。 |
| **便捷操作** | **一键触达按钮** | 前端 `localStorage` 渲染 | 1. 自动提取历史成功路径的文件名（如 `/path/to/project-foo` 提取出 `project-foo`）作为按钮文本。<br>2. 点击按钮，立刻把该路径填入输入框并自动触发“切换文件目录”逻辑。 |
| **状态查看** | **Git Status** | 带路径请求 ➔ `git status` | 后端进入当前激活的 `repoPath` 目录，执行 `git status` 并返回回显。 |
| **文件暂存** | **Git Add** | 带路径请求 ➔ `git add .` | 后端进入指定目录执行 `git add .`，成功后自动触发该目录的 Status 刷新。 |
| **代码拉取** | **Git Pull** | 带路径请求 ➔ `git pull` | 后端进入指定目录执行 `git pull`，最新同步日志回显在控制台。 |
| **一键推送** | **Git Push** | 带路径请求 ➔ `commit`+`push`| 后端进入指定目录，自动静默执行 `git commit -m "Web Auto Commit"` 并紧接着执行 `git push`。 |

---

## 4. UI/UX 界面布局

### 4.1 页面视图更新
1.  **视图 A：Web 登录锁屏页**
    *   居中登录卡片（用户名、密码输入框、登录按钮）。
2.  **视图 B：Git 主控台（登录后解锁）**
    *   **目录切换区**：包含一个文本输入框（用于粘贴路径）和一个【🔌 挂载/切换】按钮。
    *   **【新增】便捷操作区**：位于输入框正下方，呈现为一排横向排列的胶囊状按钮（例如：`[ 📁 project-A × ]` `[ 📁 project-B × ]`）。点击直接切换，点击 `×` 可删除该便捷按钮。
    *   **基础信息区**：展示当前成功挂载的路径，当前所处的分支名，以及【安全退出】按钮。
    *   **操作区**：Status、Add .、Pull、Push 四个核心动作按钮。
    *   **控制台**：黑底绿字的文本日志区块。

---

## 5. 非功能性需求
*   **路径安全校验**：后端严格使用 `path.resolve` 转化路径，防范目录越权。
*   **超时防卡死**：后端执行任何一条 Git 命令时，必须挂载 **30 秒超时强制销毁机制**。
*   **清除干净**：安全退出时同时清空内存状态，但保留 `localStorage` 中的便捷操作按钮记录，方便下次登录后直接使用。
